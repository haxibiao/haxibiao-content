type Stick {
  id: ID
  place: String
  cover: String
  stickable: Stickable @morphTo
  editorChoice: EditorChoice @belongsTo
}

union Stickable = Movie

extend type Query {
  """
  今日精选推荐
  """
  todayRecommend: [Stick] @field(resolver: "App\\Stick@resolveTodayRecommend")
}
