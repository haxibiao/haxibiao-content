extend type Mutation @middleware(checks: ["auth:api"]) {
  createPostContent(body: String!, image_urls: [String!], video_id: Int, qcvod_fileid: String, category_ids: [Int!], images: [String!]): Post!
    @field(resolver: "haxibiao\\content\\Post@resolveCreateContent")
}
